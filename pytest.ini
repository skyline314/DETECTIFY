[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = venv .venv env .env bin build dist tests/manual_verification tests/broken_tests
markers =
    performance: marks tests as performance benchmarks
    integration: marks tests requiring external services (e.g. MLflow)
pythonpath = .